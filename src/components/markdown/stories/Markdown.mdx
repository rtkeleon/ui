import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Markdown } from '../Markdown';

<Title
  title="Markdown"
  subtitle="Pass it a string as children and get markdown"
/>

<SectionTitle>Simple</SectionTitle>

String passed as child interpreted as markdown

<Preview>
  <Story id="components-markdown--simple"/>
</Preview>

<SectionTitle>Markdown Props</SectionTitle>
<Props of={ Markdown } />
